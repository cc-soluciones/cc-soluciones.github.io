---
import Badge from "@/components/ui/Badge.astro";
import { Icon } from "astro-icon/components";
import { contactContent, contactFeatures } from "@/data/contact";
---

<div class="space-y-10">
	<div class="flex justify-center">
		<Badge text={contactContent.badge} variant="light" />
	</div>

	<div class="space-y-5">
		<h2 class="max-w-xl text-4xl md:text-5xl font-bold leading-tight">
			{contactContent.title}
			<span class="text-gradient-secondary">
				{contactContent.highlight}
			</span>
		</h2>

		<p class="max-w-xl text-lg leading-relaxed text-slate-400">
			{contactContent.description}
		</p>
	</div>

	<div class="flex flex-wrap justify-center gap-4">
		{
			contactFeatures.map(({ icon, title, description }) => (
				<article class="flex min-w-48 flex-1 items-center gap-3 rounded-2xl border border-white/10 p-4 transition hover:border-white/30">
					<div class="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl border border-white/10">
						<Icon name={icon} class="w-6 h-6" />
					</div>
					<div>
						<h3 class="font-semibold text-sm">{title}</h3>
						<p class="mt-1 text-xs text-slate-400">{description}</p>
					</div>
				</article>
			))
		}
	</div>
</div>
