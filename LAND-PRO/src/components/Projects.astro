---
import Button from "@/components/ui/Button.astro";
import CardProject from "@/components/ui/CardProject.astro";

interface Props {
    id?: string;
}

const { id = "" } = Astro.props as Props;
---
<section id={id} class="my-40 w-full h-dvh pt-30" data-animation="projects">
    <div class="flex flex-wrap gap-y-25">
        <h3 class="text-5xl font-bold tracking-tight text-center w-full">
            Proyectos
        </h3>
        <CardProject image="" title="ddd" type="Landing pages"/>
        <CardProject image="" title="ddd" type="Landing pages"/>
    </div>
    <!-- <div class="reveal opacity-0 translate-y-12 transition-all duration-700 ease-out p-12 rounded-[2.5rem] text-center space-y-10 relative overflow-hidden border border-stone-800 backdrop-blur-md backdrop-saturate-150">
        <div class="absolute top-0 right-0 w-full h-100 bg-white/5 rounded-full -translate-y-1/2 translate-x-1/2 blur-3xl"/>
        <h3 class="text-5xl font-bold tracking-tight">
            ¿Tienes un proyecto en mente?
        </h3>
        <p class="text-slate-400 max-w-xl mx-auto text-xl leading-relaxed">
            Estamos listos para ayudarte a construir el futuro de tu empresa.
            Hablemos sobre cómo podemos escalar tu visión.
        </p>
        <div class="pt-4">
            <Button size="xl"> Agendar una llamada gratuita </Button>
        </div>
    </div> -->
</section>
